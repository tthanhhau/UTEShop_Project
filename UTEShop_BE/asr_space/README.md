---
title: ASR
emoji: "\U0001F399\uFE0F"
sdk: docker
pinned: false
---

# ASR (FastAPI)

POST `/transcribe` with multipart form field `audio`.
Optional form field: `language` (default `vi`).

Health check: GET `/health`.
